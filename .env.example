# Supabase Configuration
# Get these values from your Supabase project dashboard
# Go to Settings -> API to find your project URL and keys

SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here

# Service Role Key - required for schema modifications (ALTER TABLE operations)
# Get this from your Supabase project Settings -> API -> service_role key (keep this secret!)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Optional: Set custom port (default is 3000)
# PORT=3000

# ===== GOOGLE SHEETS SYNC CONFIGURATION =====
# Enable/disable Google Sheets synchronization
GOOGLE_SHEETS_SYNC_ENABLED=false

# Google Sheets ID (from the URL of your spreadsheet)
# Example: https://docs.google.com/spreadsheets/d/1ABC123DEF456/edit -> 1ABC123DEF456
GOOGLE_SHEETS_ID=your-google-sheets-id-here

# Sync interval in minutes (default: 30 minutes)
SYNC_INTERVAL_MINUTES=30

# ===== GOOGLE SHEETS API AUTHENTICATION =====
# Option 1: Service Account (Recommended for server-to-server)
# Create a service account in Google Cloud Console and download the JSON key
# Then paste the entire JSON content as a single line string
GOOGLE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"your-project",...}

# Option 2: OAuth2 (Alternative method)
# Get these from Google Cloud Console -> APIs & Services -> Credentials
# GOOGLE_CLIENT_ID=your-client-id
# GOOGLE_CLIENT_SECRET=your-client-secret
# GOOGLE_REDIRECT_URI=http://localhost:3000/auth/google/callback
# GOOGLE_REFRESH_TOKEN=your-refresh-token
